# Soundtrack files

The soundtrack files live here. The experience serves them from `/joudy/audio/` and crossfades between tracks when **Match music to each chapter** is enabled in the music controls. It is off by default, so the listener stays in control.
